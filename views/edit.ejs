<!DOCTYPE html>
<html lang="en">
<head>
   <meta charset="UTF-8">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <title>Document</title>
   <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
  <div class="w-full min-h-screen bg-zinc-900 text-white  p-10">
   <h3 class="text-4xl">Edit Your Post ✍️</h3>
   <form action="/update/<%= post._id %>" method="post">
    
    <h3 class="mt-4 text-2xl">Previous Post</h3>
    
    <input class="w-1/4 h-16 px-4 py-2 mt-4 mb-3 outline-none bg-transparent border-2 border-zinc-800 rounded-lg" type="text" value="<%= post.content %>" readonly><br>
    
    <h3 class="mt-4 text-2xl">New Post</h3>

    <input class="w-1/4 h-16 px-4 py-2 mt-4 mb-3 outline-none bg-transparent border-2 border-zinc-800 rounded-md  focus:ring-2 focus:ring-yellow-500 focus:border-transparent" name="content" type="text" value="" placeholder="Edit post" ><br>
       <input class="bg-yellow-500 px-7 py-2 rounded-lg text-black" type="submit" value="Edit Post">
   </form>
  </div>
</body>
</html>