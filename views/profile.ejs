<!DOCTYPE html>
<html lang="en">
<head>
   <meta charset="UTF-8">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <title>Document</title>
   <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
  <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
   <h3 class="text-5xl">Hello <%= user.username %>✋</h3>
   <h5 class="mb-2">You may create a new post</h5>
   <div>
   <form action="/post" method="post">   
    <textarea class="mb-4 bg-transparent border-2 text-white border-zinc-800 rounded-lg resize-none block w-full h-72" placeholder="what's on you mind?" name="content"></textarea>
    <input class=" mt-4 px-3 py-2 outline-none bg-blue-500 border-2 border-zinc-800 rounded-lg w-52 block" type="submit" value="Submit Stuffs">
    <div>
    </div> 
  </form>
</div>
<div class="flex">
  <a class="mt-4 px-3 py-2 inline-block  outline-none bg-red-500 border-2 border-zinc-800 rounded-lg w-35 " href="/logout">logout instead</a>
</div>

<div class="posts mt-10">
  <h3 class="text-2xl"> Your Posts</h3>
  <div class="postcontainer mt-5">
    <% user.post.reverse().forEach(function(posts){ %>
      <div class="post w-1/2 px-4 py-3 mt-4 border-2 border-zinc-800 rounded-lg">
        <h4 class="text-blue-500 flex text-sm tracking-tighter  flex-wrap mb-2"> @<%= user.username %> <--[Profile]</h4>
        <%= posts.content %> <--[content]
        <br>
        <small> <%= posts.Likes.length %> likes</small>    
        <div class="btns flex gap-4 mt-5">
          <a class="text-blue-500" href="/like/<%= posts._id%>">Like</a>
          <a class="text-zinc-400" href="">Edit</a>
        </div>
      </div>
    <% }) %>
    <!-- post1 -->
    

  </div>
    
  </div>
</div>

  </div>
</body>
</html>